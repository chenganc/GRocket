<ul>
    <aside>
    <section class="user_info">
	  <%= link_to gravatar_for(link.user, size: 50), link.user %>
	    <span class="title"><strong><%= link.title %> </strong></span>
	    <span class="course"><%= link.course %></span>
	    <span class="user">Posted by <%= link_to link.user.firstName %>, <%= time_ago_in_words(link.created_at) %> ago. </span>
    </section>

</ul>
