<li id="post-" <%= post.id %> >
  <span class="user"><%= link_to post.user.firstName %></span>
  <span class="title"><%= post.title %></span>
  <span class="url"><%= post.url %></span>
    <span class="timestamp">
    Posted <%= time_ago_in_words(post.created_at) %> ago.
  </span>
</li>
