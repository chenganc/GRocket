<% unless current_user?(@user) %>
  <follow_form id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
</follow_form>
<% end %>
